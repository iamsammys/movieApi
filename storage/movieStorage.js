#!/usr/bin/env node
/**
 * module to implement the movie storage
 */

movies = {};
fileName = "movies.json";


class movieStorage {
    addMovie(movie) {
        // add movie to storage
        key = ``
    }
}